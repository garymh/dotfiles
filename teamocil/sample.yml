session:
  name: "my-awesome-session"
  windows:
    - name: "test window"
      root: "~/code"
      layout: even-vertical
      filters:
        before: "rvm use 2.0.0"
        after: "echo 'I am done initializing this pane.'"
      panes:
        - cmd: "git status"
        - cmd: "ps xu"
          focus: true
        # - cmd:
        #     - "sudo service memcached start"
        #     - "sudo service mongodb start"
    - name: "testing"
      root: "~/code/nudais/rails"
      layout: even-horizontal
      panes:
        - cmd: "git status"
        - cmd: "ls"
          focus: true
        # - cmd:
        #     - "sudo service memcached start"
        #     - "sudo service mongodb start"
