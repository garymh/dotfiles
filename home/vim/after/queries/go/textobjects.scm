; extends
(keyed_element) @argument_but_in_table

(return_statement
  (_)? @return.inner) @return.outer

; all the strings on the left side of a binary expression "asdf" = b
; (binary_expression left: (interpreted_string_literal) @node)
