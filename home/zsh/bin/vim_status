#!/usr/bin/env bash

git-diff-with-abs-path() {
    local path
    path="."
    # path=$(git rev-parse --show-toplevel)
    git status --short -uno | cut -c 3- | sed "s, ,${path}/,"
}

git-diff-with-abs-path
