#!/bin/sh

vpn_status() {
  scutil --nc status "NU VPN" | grep -q Connected
  # scutil --nc status "NU VPN" | sed -n 1p | grep -qv Disconnected
}

if vpn_status; then
  echo "#[bg=colour236] #[bg=colour235]#[fg=colour152] 🔒 #[bg=colour236]"
fi
