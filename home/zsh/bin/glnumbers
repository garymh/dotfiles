#!/bin/sh

ISSUE="$(cat .git/ISSUE_NUMBER)"
MR="$(cat .git/MR_NUMBER)"

if [ "$ISSUE" = "?" ]; then
  ISSUE=""
fi

if [ "$MR" = "?" ]; then
  MR=""
fi

ECHO="#$ISSUE !$MR"

if [ "$ECHO" = "# !" ]; then
  false
else
  echo "$ECHO"
fi
