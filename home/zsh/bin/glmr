#!/bin/bash

# GITLAB_HOME="${GITLAB_HOME:-"$HOME/code/work/gdk/gitlab/"}"
# open "$(cat "$GITLAB_HOME/.git/MR_URL")"

# GITLAB_HOME="${GITLAB_HOME:-"$HOME/code/work/gdk/gitlab/"}"
open "$(cat ".git/MR_URL")"
