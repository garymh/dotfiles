# Shhh...
set -g quiet on

# vim keys
set -g mode-keys vi

# Start windows at 1 instead of 0
set -g base-index 1
set -g pane-base-index 1

# Autorename windows
set-window-option -g automatic-rename on

# clear screen
bind C send-keys -R

# Layouts
set -g main-pane-width 88
bind M select-layout main-vertical
bind E select-layout even-horizontal

source-file ~/.tmux/gary_theme.conf

##+ splitting and cycling:
unbind %
bind-key = split-window -h
bind-key - split-window -v
bind-key \ split-window -h

# # Resize current pane to 80 columns
bind 8 resize-pane -x 88

# # new stuff?
# bind g move-window # Reset window position
# bind -r Space next-window
# bind -r BSpace previous-window
# bind b break-pane
# bind B command-prompt -p "join-pane <win>.<pane>" "join-pane -t %1"
# bind N next-layout
# bind C neww -n root 'echo "sudo root shell \o/" && sudo su -l'
# # Custom
# setw -g mode-keys vi
# bind [ copy-mode
# unbind p
# bind p paste-buffer
# bind-key Escape copy-mode

# # Dedicated ssh
# bind s command-prompt -p "neww-ssh:" 'neww -n %1 "ssh %1"'
# bind S command-prompt -p "splith-ssh:" 'splitw -h "ssh %1"'
# bind Z command-prompt -p "splitz-ssh:" 'splitw -v "ssh %1"'

# # Dedicated application
# bind x command-prompt -p "neww-run:" 'neww "%1"'
# bind M command-prompt -p "neww-man:" 'neww -n "man-%1" "man %1"'
# bind T command-prompt -p "neww-tldr:" 'neww -n "tldr-%1" "tldr %1"'
# bind o neww "htop"

bind q kill-window
bind Q kill-window

# Setup 'v' to begin selection as in Vim
bind-key -t vi-copy v begin-selection
bind-key -t vi-copy y copy-pipe "reattach-to-user-namespace pbcopy"

# Update default binding of `Enter` to also use copy-pipe
unbind -t vi-copy Enter
bind-key -t vi-copy Enter copy-pipe "reattach-to-user-namespace pbcopy"

# mirror vim
bind-key -t vi-copy L end-of-line
bind-key -t vi-copy H start-of-line
bind-key -t vi-copy } halfpage-down
bind-key -t vi-copy { halfpage-up

bind-key enter copy-mode

# bind-key -t vi-copy    C-d

set -g @almost-sensible 'on'
set -g @continuum-restore 'on'
set -g @continuum-boot-options 'iterm,fullscreen'
set -g @resurrect-strategy-vim 'session'
set -g @resurrect-strategy-nvim 'session'
set -g @continuum-boot 'on'

set -g @batt_discharging_icon "💻 "
set -g @sidebar-tree-command 'fzf .'

set -g @tpm_plugins '                  \
  tmux-plugins/tpm                     \
  tmux-plugins/tmux-battery            \
  tmux-plugins/tmux-continuum          \
  tmux-plugins/tmux-copycat            \
  tmux-plugins/tmux-fpp                \
  tmux-plugins/tmux-online-status      \
  tmux-plugins/tmux-open               \
  tmux-plugins/tmux-resurrect          \
  tmux-plugins/tmux-sensible           \
  tmux-plugins/tmux-sessionist         \
  tmux-plugins/tmux-sidebar            \
  tmux-plugins/tmux-urlview            \
  tmux-plugins/tmux-yank               \
'

# initializes TMUX plugin manager
run-shell ~/.tmux/plugins/tpm/tpm

# Smart pane switching with awareness of vim splits
is_vim='echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?)(diff)?$"'
bind h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
bind j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
bind k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
bind l if-shell "$is_vim" "send-keys C-l" "select-pane -R"
bind tab if-shell "$is_vim" "send-keys C-\\" "select-pane -l"

# bind -n M-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
# bind -n M-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
# bind -n M-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
# bind -n M-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"

# bind-key        C-a send-prefix
# bind-key        C-d run-shell "/Users/garyh/.tmux/plugins/tmux-copycat/scripts/copycat_mode_start.sh '[[:digit:]]+'"
# bind-key        C-f run-shell "/Users/garyh/.tmux/plugins/tmux-copycat/scripts/copycat_mode_start.sh '(^|^\.|[[:space:]]|[[:space:]]\.|[[:space:]]\.\.|^\.\.)[[:alnum:]~_-]*/[][[:alnum:]_.#$%&+=/@-]*'"
# bind-key        C-g run-shell "/Users/garyh/.tmux/plugins/tmux-copycat/scripts/copycat_git_special.sh #{pane_current_path}"
# bind-key        Tab if-shell "echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?)(diff)?$"" "send-keys C-\" "select-pane -l"
# bind-key        C-n next-window
# bind-key        C-o rotate-window
# bind-key        C-p previous-window
# bind-key        C-r run-shell /Users/garyh/.tmux/plugins/tmux-resurrect/scripts/restore.sh
# bind-key        C-s run-shell /Users/garyh/.tmux/plugins/tmux-resurrect/scripts/save.sh
# bind-key        C-u run-shell "/Users/garyh/.tmux/plugins/tmux-copycat/scripts/copycat_mode_start.sh '(https?://|git@|git://|ssh://|ftp://|file:///)[[:alnum:]?=%/_.:,;~@!#$&()*+-]*'"
# bind-key        C-z suspend-client
# bind-key      Space next-layout
# bind-key          ! break-pane
# bind-key          " split-window
# bind-key          # list-buffers
# bind-key          $ command-prompt -I #S "rename-session '%%'"
# bind-key          & confirm-before -p "kill-window #W? (y/n)" kill-window
# bind-key          ' command-prompt -p index "select-window -t ':%%'"
# bind-key          ( switch-client -p
# bind-key          ) switch-client -n
# bind-key          , command-prompt -I #W "rename-window '%%'"
# bind-key          - split-window -v
# bind-key          . command-prompt "move-window -t '%%'"
# bind-key          / run-shell /Users/garyh/.tmux/plugins/tmux-copycat/scripts/copycat_search.sh
# bind-key          0 select-window -t :0
# bind-key          1 select-window -t :1
# bind-key          2 select-window -t :2
# bind-key          3 select-window -t :3
# bind-key          4 select-window -t :4
# bind-key          5 select-window -t :5
# bind-key          6 select-window -t :6
# bind-key          7 select-window -t :7
# bind-key          8 resize-pane -x 88
# bind-key          9 select-window -t :9
# bind-key          : command-prompt
# bind-key          ; last-pane
# bind-key          = split-window -h
# bind-key          ? list-keys
# bind-key          @ run-shell "/Users/garyh/.tmux/plugins/tmux-sessionist/scripts/tmux_promote_pane.sh '#{session_name}' '#{pane_id}' '#{pane_current_path}'"
# bind-key          C run-shell /Users/garyh/.tmux/plugins/tmux-sessionist/scripts/tmux_new_session_prompt.sh
# bind-key          D choose-client
# bind-key          E select-layout even-horizontal
# bind-key          I run-shell "/Users/garyh/.tmux/plugins/tpm/scripts/install_plugins.sh >/dev/null 2>&1"
# bind-key          L switch-client -l
# bind-key          M select-layout main-vertical
# bind-key          Q kill-window
# bind-key          R run-shell " \
#       tmux source-file ~/.tmux.conf > /dev/null; \
#       tmux display-message "Sourced .tmux.conf!""
# bind-key          S switch-client -l
# bind-key          U run-shell /Users/garyh/.tmux/plugins/tpm/scripts/update_plugin_prompt.sh
# bind-key          [ copy-mode
# bind-key          \ split-window -h
# bind-key          ] paste-buffer
# bind-key          a last-window
# bind-key          c new-window
# bind-key          d detach-client
# bind-key          f capture-pane ; save-buffer /tmp/tmux-buffer ; new-window -c #{pane_current_path} "sh -c 'cat /tmp/tmux-buffer | fpp && rm /tmp/tmux-buffer'"
# bind-key          g run-shell /Users/garyh/.tmux/plugins/tmux-sessionist/scripts/tmux_goto_session.sh
# bind-key          h if-shell "echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?)(diff)?$"" "send-keys C-h" "select-pane -L"
# bind-key          i display-message
# bind-key          j if-shell "echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?)(diff)?$"" "send-keys C-j" "select-pane -D"
# bind-key          k if-shell "echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?)(diff)?$"" "send-keys C-k" "select-pane -U"
# bind-key          l if-shell "echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?)(diff)?$"" "send-keys C-l" "select-pane -R"
# bind-key          n next-window
# bind-key          o select-pane -t :.+
# bind-key          p previous-window
# bind-key          q kill-window
# bind-key          r refresh-client
# bind-key          s choose-tree
# bind-key          t clock-mode
# bind-key          u capture-pane -J ; save-buffer /var/folders/q5/99hpdmc91x17r24pxb5sc5l00000gn/T//tmux-buffer ; delete-buffer ; split-window -l 10 "urlview '/var/folders/q5/99hpdmc91x17r24pxb5sc5l00000gn/T//tmux-buffer'"
# bind-key          w choose-window
# bind-key          x confirm-before -p "kill-pane #P? (y/n)" kill-pane
# bind-key          y run-shell /Users/garyh/.tmux/plugins/tmux-yank/scripts/copy_line.sh
# bind-key          z resize-pane -Z
# bind-key          { swap-pane -U
# bind-key          } swap-pane -D
# bind-key          ~ show-messages
# bind-key     BSpace run-shell "/Users/garyh/.tmux/plugins/tmux-sidebar/scripts/toggle.sh 'fzf . | sh -c "LESS= less --dumb --chop-long-lines --tilde --IGNORE-CASE --RAW-CONTROL-CHARS",left,40,focus' '#{pane_id}'"
# bind-key      PPage copy-mode -u
# bind-key -r      Up select-pane -U
# bind-key -r    Down select-pane -D
# bind-key -r    Left select-pane -L
# bind-key -r   Right select-pane -R
# bind-key        M-1 select-layout even-horizontal
# bind-key        M-2 select-layout even-vertical
# bind-key        M-3 select-layout main-horizontal
# bind-key        M-4 select-layout main-vertical
# bind-key        M-5 select-layout tiled
# bind-key        M-h run-shell "/Users/garyh/.tmux/plugins/tmux-copycat/scripts/copycat_mode_start.sh '\b[0-9a-f]{7,40}\b'"
# bind-key        M-i run-shell "/Users/garyh/.tmux/plugins/tmux-copycat/scripts/copycat_mode_start.sh '[[:digit:]]{1,3}\.[[:digit:]]{1,3}\.[[:digit:]]{1,3}\.[[:digit:]]{1,3}'"
# bind-key        M-n next-window -a
# bind-key        M-o rotate-window -D
# bind-key        M-p previous-window -a
# bind-key        M-u run-shell /Users/garyh/.tmux/plugins/tpm/scripts/clean_plugins.sh
# bind-key -r    M-Up resize-pane -U 5
# bind-key -r  M-Down resize-pane -D 5
# bind-key -r  M-Left resize-pane -L 5
# bind-key -r M-Right resize-pane -R 5
# bind-key -r    C-Up resize-pane -U
# bind-key -r  C-Down resize-pane -D
# bind-key -r  C-Left resize-pane -L
# bind-key -r C-Right resize-pane -R

# bind-key -t vi-copy    C-b page-up
# bind-key -t vi-copy    C-c cancel
# bind-key -t vi-copy    C-d halfpage-down
# bind-key -t vi-copy    C-e scroll-down
# bind-key -t vi-copy    C-f page-down
# bind-key -t vi-copy    C-h cursor-left
# bind-key -t vi-copy    C-j copy-selection
# bind-key -t vi-copy  Enter copy-pipe "reattach-to-user-namespace pbcopy"
# bind-key -t vi-copy    C-o copy-pipe "xargs -I {} tmux send-keys 'vim -- "{}"'; tmux send-keys 'C-m'"
# bind-key -t vi-copy    C-u halfpage-up
# bind-key -t vi-copy    C-y scroll-up
# bind-key -t vi-copy Escape clear-selection
# bind-key -t vi-copy  Space begin-selection
# bind-key -t vi-copy      ! copy-pipe "tr -d '\n' | reattach-to-user-namespace pbcopy"
# bind-key -t vi-copy      " start-named-buffer
# bind-key -t vi-copy      $ end-of-line
# bind-key -t vi-copy      , jump-reverse
# bind-key -t vi-copy      / search-forward
# bind-key -t vi-copy      0 start-of-line
# bind-key -t vi-copy      1 start-number-prefix
# bind-key -t vi-copy      2 start-number-prefix
# bind-key -t vi-copy      3 start-number-prefix
# bind-key -t vi-copy      4 start-number-prefix
# bind-key -t vi-copy      5 start-number-prefix
# bind-key -t vi-copy      6 start-number-prefix
# bind-key -t vi-copy      7 start-number-prefix
# bind-key -t vi-copy      8 start-number-prefix
# bind-key -t vi-copy      9 start-number-prefix
# bind-key -t vi-copy      : goto-line
# bind-key -t vi-copy      ; jump-again
# bind-key -t vi-copy      ? search-backward
# bind-key -t vi-copy      A append-selection
# bind-key -t vi-copy      B previous-space
# bind-key -t vi-copy      D copy-end-of-line
# bind-key -t vi-copy      E next-space-end
# bind-key -t vi-copy      F jump-backward
# bind-key -t vi-copy      G history-bottom
# bind-key -t vi-copy      H start-of-line
# bind-key -t vi-copy      J scroll-down
# bind-key -t vi-copy      K scroll-up
# bind-key -t vi-copy      L end-of-line
# bind-key -t vi-copy      M middle-line
# bind-key -t vi-copy      N search-reverse
# bind-key -t vi-copy      T jump-to-backward
# bind-key -t vi-copy      V select-line
# bind-key -t vi-copy      W next-space
# bind-key -t vi-copy      Y copy-pipe "tmux paste-buffer"
# bind-key -t vi-copy      ^ back-to-indentation
# bind-key -t vi-copy      b previous-word
# bind-key -t vi-copy      e next-word-end
# bind-key -t vi-copy      f jump-forward
# bind-key -t vi-copy      g history-top
# bind-key -t vi-copy      h cursor-left
# bind-key -t vi-copy      j cursor-down
# bind-key -t vi-copy      k cursor-up
# bind-key -t vi-copy      l cursor-right
# bind-key -t vi-copy      n search-again
# bind-key -t vi-copy      o copy-pipe "xargs -I {} tmux run-shell 'cd #{pane_current_path}; open "{}" > /dev/null'"
# bind-key -t vi-copy      q cancel
# bind-key -t vi-copy      t jump-to-forward
# bind-key -t vi-copy      v begin-selection
# bind-key -t vi-copy      w next-word
# bind-key -t vi-copy      y copy-pipe "reattach-to-user-namespace pbcopy"
# bind-key -t vi-copy BSpace cursor-left
# bind-key -t vi-copy  NPage page-down
# bind-key -t vi-copy  PPage page-up
# bind-key -t vi-copy     Up cursor-up
# bind-key -t vi-copy   Down cursor-down
# bind-key -t vi-copy   Left cursor-left
# bind-key -t vi-copy  Right cursor-right
# bind-key -t vi-copy    M-y copy-pipe "reattach-to-user-namespace pbcopy; tmux paste-buffer"
# bind-key -t vi-copy   C-Up scroll-up
# bind-key -t vi-copy C-Down scroll-down
