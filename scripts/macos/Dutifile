# http://duti.org/documentation.html
# This is a duti config file to set default applications for file types or extensions (UTIs)

# Blank lines and lines beginning with a '#' are ignored
# If the file doesn't end with a blank line, duti might give a "line too long" error.

# To know the bundle id of a specific app:
# osascript -e "id of app \"<app_name>\""

# To know the UTI from a specific file:
# mdls <filepath> | awk '/kMDItemContentType /{print $NF}'

# List of Uniform Type Identifiers in OS X:
# https://developer.apple.com/library/mac/#documentation/Miscellaneous/Reference/UTIRef/Articles/System-DeclaredUniformTypeIdentifiers.html

# Format:
# [Bundle ID] [ UTI | ext | MIME type ] [role]

# Valid roles:
#
# all     app handles all roles for the given UTI.
# viewer  app handles reading and displaying documents with given UTI.
# editor  app can manipulate and save the item. Implies viewer.
# shell   app can execute the item.
# none    app cannot open the item, but provides an icon for the given UTI.

app.loshadki.OpenIn.v4 net.daringfireball.markdown all
app.loshadki.OpenIn.v4 com.apple.applescript.text all
app.loshadki.OpenIn.v4 com.netscape.javascript-source all
app.loshadki.OpenIn.v4 com.sun.java-source all
app.loshadki.OpenIn.v4 org.go.source all
app.loshadki.OpenIn.v4 public.c-plus-plus-source all
app.loshadki.OpenIn.v4 public.data all
app.loshadki.OpenIn.v4 public.php-script all
app.loshadki.OpenIn.v4 public.plain-text all
app.loshadki.OpenIn.v4 public.python-script all
app.loshadki.OpenIn.v4 public.ruby-script all
app.loshadki.OpenIn.v4 public.shell-script all
app.loshadki.OpenIn.v4 public.source-code all
app.loshadki.OpenIn.v4 public.unix-executable all
app.loshadki.OpenIn.v4 public.yaml all
app.loshadki.OpenIn.v4 public.xml all
app.loshadki.OpenIn.v4 text/plain all
app.loshadki.OpenIn.v4 .json all
app.loshadki.OpenIn.v4 .zsh all
# at.eggerapps.tabletool public.comma-separated-values-text all
# org.hammerspoon.Hammerspoon .torrent all

app.loshadki.OpenIn.v4 .avi all
app.loshadki.OpenIn.v4 .mkv all
app.loshadki.OpenIn.v4 .mp4 all
app.loshadki.OpenIn.v4 .m4v all
app.loshadki.OpenIn.v4 .mov all

app.loshadki.OpenIn.v4 .stl all
app.loshadki.OpenIn.v4 .3mf all

# bundle id URL scheme
com.reederapp.mac feed

# last line must be blank
